// No backend setup required: the app runs entirely on a localStorage-backed
// local store that seeds itself with demo data on first access in the browser.
async function globalSetup() {
  return;
}

export default globalSetup;
